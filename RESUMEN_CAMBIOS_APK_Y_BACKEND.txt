RESUMEN DE CAMBIOS REALIZADOS (APK + BACKEND + AWS)
Fecha: 2026-04-30
Proyecto: e-Go

1) OBJETIVO GENERAL
- Generar APK funcional para compartir.
- Corregir errores de backend que devolvian "Not Found" y despues "500".
- Ajustar branding (nombre e icono) en Android.
- Verificar conectividad real con API Gateway + Lambda en AWS.

2) CAMBIOS EN FRONTEND (ANDROID / EXPO)
- Se corrigio configuracion para build release Android.
- Se confirmo generacion correcta de APK release:
  frontend/android/app/build/outputs/apk/release/app-release.apk
- Se ajusto el nombre visible de la app en Android:
  archivo: frontend/android/app/src/main/res/values/strings.xml
  app_name -> "E-Go"
- Se reviso y ajusto la configuracion de icono en:
  archivo: frontend/app.json
- Para evitar recorte del icono adaptativo, se aplico inset del logo en launcher:
  archivos:
  - frontend/android/app/src/main/res/drawable/ic_launcher_foreground_inset.xml
  - frontend/android/app/src/main/res/drawable/ic_launcher_monochrome_inset.xml
  - frontend/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - frontend/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
- Se recompilo y reinstalo varias veces en dispositivo fisico para validar cambios visuales.

3) CAMBIOS EN CONFIGURACION DE API (FRONTEND)
- Se verifico que la API correcta NO usa /prod para este entorno.
- URL valida confirmada:
  https://ge8c9gux3d.execute-api.eu-north-1.amazonaws.com
- Se ajusto frontend/.env para usar la base correcta sin /prod.

4) DIAGNOSTICO EN AWS (API GATEWAY + LAMBDA)
- API Gateway verificado:
  API ID: ge8c9gux3d
  Nombre: ego-api-v2
  Endpoint: https://ge8c9gux3d.execute-api.eu-north-1.amazonaws.com
- Ruteo configurado con $default a integracion Lambda backend-ego-api.
- Al principio, /auth/local/login devolvia 404 (ruta no presente en version desplegada).
- Tras actualizar Lambda, la ruta empezo a responder pero con 500.
- Logs de CloudWatch identificaron error real:
  Runtime.ImportModuleError: Cannot find module '../middleware/requireAdmin'

5) FIX CRITICO EN EMPAQUETADO LAMBDA
- Se detecto que el zip de despliegue NO incluia la carpeta middleware.
- Se corrigio script de empaquetado:
  archivo: backend/scripts/build-lambda-zip.js
  cambio: se agrego "middleware" a DIRS_TO_COPY.
- Tras redeploy de Lambda con zip corregido, desaparecio el 500 de import.

6) VALIDACION FINAL DE ENDPOINTS
- /auth/local/login:
  - antes: 404
  - despues de deploy parcial: 500
  - despues del fix de middleware: funciona correctamente
- Pruebas finales:
  - register -> "Este email ya esta registrado" (comportamiento esperado)
  - login -> devuelve usuario correctamente (comportamiento esperado)

7) INSTALACION Y PRUEBAS EN MOVIL
- APK instalada en dispositivo via adb.
- Se verifico apertura de la app.
- Se verifico funcionamiento de backend tras correccion y despliegue.

8) GIT Y RAMA
- Se creo una rama nueva llamada: APK
- Commit generado en esa rama para guardar cambios locales pendientes.

9) COMANDOS CLAVE UTILIZADOS (RESUMEN)
- Build release APK:
  .\gradlew.bat app:assembleRelease -x lint -x lintVitalAnalyzeRelease -x lintVitalRelease -x test --no-daemon
- Instalacion en dispositivo:
  adb install -r frontend/android/app/build/outputs/apk/release/app-release.apk
- Diagnostico AWS:
  aws apigatewayv2 get-apis --region eu-north-1
  aws apigatewayv2 get-routes --api-id ge8c9gux3d --region eu-north-1
  aws logs tail "/aws/lambda/backend-ego-api" --since 5m --region eu-north-1

10) ESTADO ACTUAL
- Login local backend: FUNCIONANDO.
- API base correcta sin /prod: CONFIRMADA.
- APK release: GENERADA e INSTALABLE.
- Icono: ajustado para minimizar recorte en launcher.


/** Alinea el package Android con app.json (com.ego.app) para autolinking / BuildConfig. */
module.exports = {
  project: {
    android: {
      sourceDir: './android',
      packageName: 'com.ego.app',
    },
  },
};
